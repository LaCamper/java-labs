# Лабораторна робота №2. Опрацювання масивів із застосуванням циклів

**Предметна область:** Спортивне спорядження (Sports Equipment)

## Опис сутності
Клас `SportEquipment` представляє одиницю спортивного інвентарю та містить такі поля:
- `name` (String) — назва товару (наприклад, "Гантеля").
- `category` (String) — категорія спорту (наприклад, "Фітнес").
- `price` (double) — ціна товару в гривнях.

## Реалізований функціонал
- **Рівень 1 (Базовий):** Введення масиву об'єктів з клавіатури через `Scanner`, виведення через цикл `for-each`. Реалізовано підрахунок кількості товарів, ціна яких менша за вказане користувачем значення.
- **Рівень 2 (Середній):** Сортування масиву об'єктів за зростанням ціни методом простого обміну (Bubble Sort) без використання вбудованих бібліотек.
- **Рівень 3 (Високий):** Лінійний пошук точного збігу об'єкта в масиві. Пошук здійснюється за всіма полями за допомогою перевизначеного методу `equals()`.

## Інструкція запуску
1. Відкрийте проєкт у вашому IDE (наприклад, VS Code або IntelliJ IDEA).
2. Перейдіть до файлу `Main.java` у пакеті `javalabs.lab2.sportsequipment`.
3. Запустіть метод `main`.
4. Дотримуйтесь інструкцій у консолі для введення даних (зверніть увагу: залежно від локалі ОС дробові числа можуть вводитися через кому `,` або крапку `.`).